# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# docs/ is the tracked documentation home; ignore only local/generated artifacts there
# (the GitHub Pages site is built in CI, not committed; sample.bpmn is a local scratch fixture)
/docs/index.html
/docs/assets/
/docs/sample.bpmn

# OS files
.DS_Store
Thumbs.db

# IDE / Editor
.idea/
.vscode/*
# …but keep the shared, committed editor config (tasks/recommended extensions/settings)
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
*.swp
*.swo
*~

# Environment / secrets
.env
.env.*

# Logs
*.log
npm-debug.log*

# Cache
.cache/
.vitest/
.eslintcache

# Coverage reports
coverage/

# Package tarballs
*.tgz

# Local-only analysis report (intentionally not committed)
/CODE-HEALTH-TOOLING-REPORT.md
